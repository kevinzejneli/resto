export default function InventoryPage() {
  return (
    <>
      <h1>Inventory</h1>
      <p>Stock levels, movements, and low-stock alerts.</p>
      <div className="placeholder">
        Domain types live in <code>@resto/core</code> (
        <code>InventoryService</code>). UI to be built.
      </div>
    </>
  );
}
